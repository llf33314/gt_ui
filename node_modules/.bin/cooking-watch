#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cooking/bin/cooking-watch" "$@"
  ret=$?
else 
  node  "$basedir/../cooking/bin/cooking-watch" "$@"
  ret=$?
fi
exit $ret
