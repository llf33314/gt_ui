<template><section><h2 id="custom-theme"><a class="header-anchor" href="#custom-theme" aria-hidden="true">¶</a> Custom theme</h2>
<p>Element uses BEM-styled CSS so that you can override styles easily. But if you need to replace styles at a large scale, e.g. change the theme color from blue to orange or green, maybe overriding them one by one is not a good idea, and this is where our theme customization tools kick in.</p>
<h2 id="changing-theme-color"><a class="header-anchor" href="#changing-theme-color" aria-hidden="true">¶</a> Changing theme color</h2>
<p>If you just want to change the theme color of Element, the <a href="https://elementui.github.io/theme-preview/#/en-US">theme preview website</a> is recommended.</p>
<p>The main color of Element is bright and friendly blue. By changing it, you can make Element more visually connected to specific projects.</p>
<p>The above website enables you to preview theme of a new theme color in real-time, and it can generate a complete style package based on the new theme color for you to download directly (to import new style files in your project, please refer to the 'Import custom theme' or 'Import component theme on demand' part of this section).</p>
<h2 id="more-customizations"><a class="header-anchor" href="#more-customizations" aria-hidden="true">¶</a> More customizations</h2>
<p>If you need more customization than just changing the theme color, please follow these steps:</p>
<h3 id="install-related-tool"><a class="header-anchor" href="#install-related-tool" aria-hidden="true">¶</a> Install related tool</h3>
<p>First install the theme generator globally or locally. Local install is recommended because in this way, when others clone your project, npm will automatically install it for them.</p>
<pre><code class="hljs language-shell">npm i element-theme -g
</code></pre>
<p>Then install the default theme from npm or GitHub.</p>
<pre><code class="hljs language-shell"># from npm
npm i element-theme-default -D

# from GitHub
npm i https://github.com/ElementUI/theme-default -D
</code></pre>
<h3 id="initialize-variable-file"><a class="header-anchor" href="#initialize-variable-file" aria-hidden="true">¶</a> Initialize variable file</h3>
<p>After successfully installing the above packages, a command named <code>et</code> is available in CLI (if the packages are installed locally, use <code>node_modules/.bin/et</code> instead). Run <code>-i</code> to initialize the variable file which outputs to <code>element-variables.css</code> by default. And you can specify its output directory as you will.</p>
<pre><code class="hljs language-shell">et -i [custom output file]

&gt; ✔ Generator variables file
</code></pre>
<p>In <code>element-variables.css</code> you can find all the variables we used to style Element and they are defined in CSS4 style:</p>
<pre><code class="hljs language-css"><span class="hljs-selector-pseudo">:root</span> {

  <span class="hljs-comment">/* Colors
  -------------------------- */</span>
  <span class="hljs-attribute">--color-primary</span>: <span class="hljs-number">#20a0ff</span>;
  <span class="hljs-attribute">--color-success</span>: <span class="hljs-number">#13ce66</span>;
  <span class="hljs-attribute">--color-warning</span>: <span class="hljs-number">#f7ba2a</span>;
  <span class="hljs-attribute">--color-danger</span>: <span class="hljs-number">#ff4949</span>;
  <span class="hljs-attribute">--color-info</span>: <span class="hljs-number">#50BFFF</span>;
  <span class="hljs-attribute">--color-blue</span>: <span class="hljs-number">#2e90fe</span>;
  <span class="hljs-attribute">--color-blue-light</span>: <span class="hljs-number">#5da9ff</span>;
  <span class="hljs-attribute">--color-blue-lighter</span>: <span class="hljs-built_in">rgba</span>(var(--color-blue), <span class="hljs-number">0.12</span>);
  <span class="hljs-attribute">--color-white</span>: <span class="hljs-number">#fff</span>;
  <span class="hljs-attribute">--color-black</span>: <span class="hljs-number">#000</span>;
  <span class="hljs-attribute">--color-grey</span>: <span class="hljs-number">#C0CCDA</span>;
</code></pre>
<h3 id="modify-variables"><a class="header-anchor" href="#modify-variables" aria-hidden="true">¶</a> Modify variables</h3>
<p>Just edit <code>element-variables.css</code>, e.g. changing the theme color to red:</p>
<pre><code class="hljs language-CSS"><span class="hljs-selector-tag">--color-primary</span>: <span class="hljs-selector-tag">red</span>;
</code></pre>
<h3 id="build-theme"><a class="header-anchor" href="#build-theme" aria-hidden="true">¶</a> Build theme</h3>
<p>After saving the variable file, use <code>et</code> to build your theme. You can activate <code>watch</code> mode by adding a parameter <code>-w</code>. And if you customized the variable file's output, you need to add a parameter <code>-c</code> and variable file's name:</p>
<pre><code class="hljs language-shell">et

&gt; ✔ build theme font
&gt; ✔ build element theme
</code></pre>
<h3 id="import-custom-theme"><a class="header-anchor" href="#import-custom-theme" aria-hidden="true">¶</a> Import custom theme</h3>
<p>By default the build theme file is placed inside <code>./theme</code>. You can specify its output directory with parameter <code>-o</code>. Importing your own theme is just like importing the default theme, only this time you import the file you just built:</p>
<pre><code class="hljs language-javascript"><span class="hljs-keyword">import</span> <span class="hljs-string">'../theme/index.css'</span>
<span class="hljs-keyword">import</span> ElementUI <span class="hljs-keyword">from</span> <span class="hljs-string">'element-ui'</span>
<span class="hljs-keyword">import</span> Vue <span class="hljs-keyword">from</span> <span class="hljs-string">'vue'</span>

Vue.use(ElementUI)
</code></pre>
<h3 id="import-component-theme-on-demand"><a class="header-anchor" href="#import-component-theme-on-demand" aria-hidden="true">¶</a> Import component theme on demand</h3>
<p>If you are using <code>babel-plugin-component</code> for on-demand import, just modify <code>.babelrc</code> and specify <code>styleLibraryName</code> to the path where your custom theme is located relative to <code>.babelrc</code>. Note that <code>~</code> is required:</p>
<pre><code class="hljs language-json">{
  <span class="hljs-attr">"plugins"</span>: [[<span class="hljs-string">"component"</span>, [
    {
      <span class="hljs-attr">"libraryName"</span>: <span class="hljs-string">"element-ui"</span>,
      <span class="hljs-attr">"styleLibraryName"</span>: <span class="hljs-string">"~theme"</span>
    }
  ]]]
}
</code></pre>
<p>If you are unfamiliar with <code>babel-plugin-component</code>, please refer to <a href="./#/en-US/component/quickstart">Quick Start</a>. For more details, check out the <a href="https://github.com/ElementUI/element-theme">project repository</a> of <code>element-theme</code>.</p>
</section></template>

